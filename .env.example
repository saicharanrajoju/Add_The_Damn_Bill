# Server-side only — set this in Vercel dashboard under Project Settings → Environment Variables
# Never prefix with VITE_ (that would expose it in the client bundle)
GEMINI_API_KEY=your_gemini_api_key_here
